## Setup
- If not already install uv: `https://docs.astral.sh/uv/getting-started/installation/`
- init/setup: `uv sync`


`git pull && sh ./run-on-ai-lab.sh && tail -n 50 -f my_job.out`
`squeue --me`
`nodesummary`
`scancel <jobid>`
